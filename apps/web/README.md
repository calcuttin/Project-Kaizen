# Kaizen web client

The current Vite client remains at the repository root while modules move here incrementally. Shared domain contracts and import parsers already live in `packages/`; moving the application entry point is deferred to avoid breaking existing clone workflows.

Run install, development, and build commands from the repository root. Start with the [README](../../README.md), [local setup](../../docs/local-setup.md), or [cloud setup](../../docs/cloud-setup.md).
